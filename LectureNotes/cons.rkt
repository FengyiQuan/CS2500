;; The first three lines of this file were inserted by DrRacket. They record metadata
;; about the language level of this file in a form that our tools can easily process.
#reader(lib "htdp-beginner-reader.ss" "lang")((modname cons) (read-case-sensitive #t) (teachpacks ()) (htdp-settings #(#t constructor repeating-decimal #f #t none #f () #f)))
; cons : Any List -> List
; '() <- empty list!

; first : ConsList -> Any ; substract the first item in the list
; rest : ConsList -> List ; the rest of substracting the first item in the list

; list?, cons?, empty?


(define LIST-0 '())
(define LIST-1 (cons 4 LIST-0))
(define LIST-2 (cons 3 LIST-1))





